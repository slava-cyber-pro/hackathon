global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "budgetsphere-backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: "backend"
